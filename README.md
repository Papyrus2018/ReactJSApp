#Zion App

##Setup

To start the application in development mode:
    `npm run dev`

To start the application on a device (emulator)
    `cordova run ios`
    `cordova run android`
    `cordova run browser`

